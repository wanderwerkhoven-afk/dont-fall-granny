import {readFileSync,writeFileSync} from 'node:fs';
import assert from 'node:assert/strict';
const file='index.html';let html=readFileSync(file,'utf8');
const marker='/* Agent UI/UX review 2026-09-22: accessible and aligned store */';
assert.ok(!html.includes(marker),'UI review already applied; do not duplicate');
function replaceOnce(oldText,newText){assert.equal(html.split(oldText).length-1,1,'Expected one anchor: '+oldText.slice(0,85));html=html.replace(oldText,newText)}
const css=`\n${marker}\n/* Store cards have aligned actions; buttons remain easy to tap. */\n#overlay.storepage .store-item{min-width:0;justify-content:flex-start;text-align:center}\n#overlay.storepage .store-item b{line-height:1.25;overflow-wrap:anywhere}\n#overlay.storepage .store-item small{flex:1;line-height:1.35;overflow-wrap:anywhere}\n#overlay.storepage .store-item button{margin-top:auto;min-height:44px;cursor:pointer;line-height:1.2}\n#overlay.storepage .store-item button:disabled{cursor:default}\n/* Keep navigation discoverable as the wardrobe panel scrolls. */\n#overlay.storepage .menu-tabs{position:sticky;top:0;z-index:11;background:#fff8ec;padding:5px 0;border-radius:12px}\n#overlay.storepage .panel{scroll-padding-top:62px;overscroll-behavior:contain}\n/* Visible keyboard focus must not be hidden by the decorative rail. */\n#overlay button:focus-visible,#overlay .panel:focus-visible{outline:3px solid #256f9e;outline-offset:3px}\n#overlay.storepage .store-item:focus-within{border-color:#256f9e;box-shadow:0 0 0 2px #256f9e44}\n@media(max-width:650px){#overlay.storepage .menu-tabs button{min-height:44px}#overlay.storepage .store-item{padding:9px 7px;gap:6px}#overlay.storepage .store-item button{font-size:12px;padding:7px 4px}}\n@media(prefers-reduced-motion:reduce){#overlay.storepage .menu-tabs{scroll-behavior:auto}}\n`;
replaceOnce('</style>',css+'</style>');
replaceOnce('<div class="overlay" id="overlay"><div class="panel">','<div class="overlay" id="overlay"><div class="panel" tabindex="0" aria-label="Spelmenu en winkel, scroll met pijltjestoetsen of veeg">');
const tabs="menuTabs.querySelectorAll('button').forEach(b=>b.classList.toggle('active',b.dataset.page===page));";
replaceOnce(tabs,"menuTabs.querySelectorAll('button').forEach(b=>{const active=b.dataset.page===page;b.classList.toggle('active',active);b.setAttribute('aria-pressed',String(active))});");
const restore="if(page==='clothes'||page==='gadgets'){menuPanel.scrollTop=previousScroll;requestAnimationFrame(syncClothesHanger)}";
replaceOnce(restore,"if(page==='clothes'||page==='gadgets'){menuPanel.scrollTop=previousScroll;requestAnimationFrame(syncClothesHanger);const updated=menuContent.querySelector('[data-buy=\"'+item.id+'\"]');if(updated&&page==='clothes')updated.focus({preventScroll:true})}");
const script=html.match(/<script>\s*([\s\S]*?)\s*<\/script>/)?.[1];assert.ok(script);new Function(script);
writeFileSync(file,html);console.log('PASS: UI review applied; store card alignment, focus, keyboard scroll, sticky tabs and post-purchase focus');